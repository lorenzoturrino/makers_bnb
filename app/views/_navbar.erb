<nav class="navbar flexbox">
  <% if current_user %>

    <section class="navbar-welcome">
      <h1>Welcome <%= current_user.name %></h1>
    </section>

    <section>

      <% unless current_path == '/spaces' %>
        <form action='/spaces' method="get">
          <button type="submit">Spaces</button>
        </form>
      <%end%>

      <% unless current_path == '/requests' %>
        <form action='/requests'>
          <button type="submit">Requests</button>
        </form>
      <%end%>

      <form action='/session' method="post">
        <input type='hidden' name='_method' value='delete'>
        <button type="submit">Sign out</button>
      </form>

  <%else%>

    <section class="navbar-welcome">
      <h1>Makers BnB</h1>
    </section>
      <section class="navbar-nav">

        <% unless current_path == '/session/new' %>
          <form action='/session/new'>
          <button type="submit">Log in</button>
          </form>
        <%end%>

        <% unless current_path == '/user/new' %>
          <form action='/user/new'>
          <button type="submit">Register</button>
          </form>
        <%end%>

  <%end%>

    </section>

</nav>
